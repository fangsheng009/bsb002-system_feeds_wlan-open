diff --git a/net/mac80211/chan.c b/net/mac80211/chan.c
index c7c5142..0b79097 100644
--- a/net/mac80211/chan.c
+++ b/net/mac80211/chan.c
@@ -814,7 +814,7 @@ int ieee80211_vif_reserve_chanctx(struct ieee80211_sub_if_data *sdata,
 				  bool radar_required)
 {
 	struct ieee80211_local *local = sdata->local;
-	struct ieee80211_chanctx *new_ctx, *curr_ctx, *ctx;
+	struct ieee80211_chanctx *new_ctx = NULL, *curr_ctx, *ctx;
 
 	lockdep_assert_held(&local->chanctx_mtx);
 
@@ -822,7 +822,9 @@ int ieee80211_vif_reserve_chanctx(struct ieee80211_sub_if_data *sdata,
 	if (curr_ctx && local->use_chanctx && !local->ops->switch_vif_chanctx)
 		return -ENOTSUPP;
 
-	new_ctx = ieee80211_find_reservation_chanctx(local, chandef, mode);
+	if (!curr_ctx || local->use_chanctx)
+		new_ctx = ieee80211_find_reservation_chanctx(local, chandef, mode);
+
 	if (!new_ctx) {
 		if (ieee80211_can_create_new_chanctx(local)) {
 			new_ctx = ieee80211_new_chanctx(local, chandef, mode);
